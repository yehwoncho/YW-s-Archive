# YW's Archive — Design Guide

| 항목 | 내용 |
|---|---|
| 서비스명 | YW's Archive |
| 문서 버전 | v2.0 (Y2K 리디자인) |
| 이전 버전 | v1.0 — 따뜻한 종이형 (폐기) |
| 작성일 | 2026-08-13 |
| 관련 문서 | prd.md |

---

## 1. 디자인 원칙

이 문서는 "예쁘게 만드는 법"이 아니라 **"헷갈릴 때 무엇을 기준으로 정할지"**를 적어둔 것이다.
비전공자 기준으로 작성했으므로, 판단이 서지 않으면 아래 세 원칙 순서대로 따른다.

### 원칙 1 — 화려함은 포인트에만 쓴다
넣을지 말지 고민되면, "정보를 전달하는 자리인가 장식하는 자리인가"부터 구분한다.
장식 자리면 과감하게, 정보 자리(본문·과정 서술·연락처)면 가독성을 우선한다.
Y2K 톤이라고 모든 요소에 네온과 글로우를 두르지 않는다.

### 원칙 2 — 화면이 아니라 모니터처럼
배경은 어두운 블루~퍼플. 종이가 아니라 **2000년대 초반 CRT 모니터 화면**처럼 보이는 것이 목표다.
글로우, 스캔라인, 격자는 그 시절 디지털 화면이 실제로 그렇게 보였기 때문에 쓰는 것이지 장식을 위한 장식이 아니다.

### 원칙 3 — 비어 있어도 신호는 켜져 있는 것처럼
현재 프로젝트 8개가 모두 준비 중이다. Y2K 톤에서는 "절제된 여백"보다 **"신호는 오고 있다"**는
인상이 더 톤에 맞는다. `Coming Soon` 배지에 은은한 글로우를 넣어 "꺼진 화면"이 아니라
"대기 중인 화면"으로 읽히게 한다. 단, 이 신호도 절제해야 한다 — 화면 전체가 깜빡이면 소음이 된다.

---

## 2. 무드

**Y2K 사이버.**

| 지향 | 회피 |
|---|---|
| 디지털 첫인상이 뚜렷한 | 밋밋하고 밍밍한 |
| 크롬·메탈릭 질감 | 평면적인 플랫 디자인 |
| 네온 글로우로 살아있는 | 정적이고 무표정한 |
| 로우파이 UI의 손맛 | 지나치게 매끈한 대기업 UI |
| 장난기 있지만 읽히는 | 화려함에 정보가 묻히는 |

**한 줄 기준** — "2000년대 초반 개인 홈페이지를 지금 감각으로 다시 만든" 느낌.
실제 특정 브랜드·아티스트의 로고·사진·그래픽은 쓰지 않는다. **시대의 양식만** 참고한다.

---

## 3. 컬러

### 3.1 팔레트 (딥 사이버 & 네온) — 총 8색

| 이름 | 값 | 용도 |
|---|---|---|
| 배경 | `#0B0E2E` | 페이지 전체 바탕 (딥 네이비-퍼플) |
| 표면 | `#171A45` | 카드, 모달, 인풋 등 떠 있는 면 |
| 글자 | `#F1EEFA` | 제목, 본문 등 기본 텍스트 |
| 보조 글자 | `#A6A3D1` | 설명문, 캡션, 부가 정보 |
| 테두리 | `#34386E` | 카드 외곽선, 구분선, 기본 상태 |
| 포인트 — 라임 | `#C6FF4A` | 1순위 강조: 링크, `Coming Soon` 배지, 주요 CTA |
| 포인트 — 핫핑크 | `#FF3FA4` | 2순위 강조: 호버, 스티커 라벨, 보조 강조 |
| 포인트 — 사이버 블루 | `#33E1FF` | 3순위 강조: 포커스 링, 스캔라인, 격자선, 글로우 |

### 3.2 사용 규칙

| 규칙 | 내용 |
|---|---|
| 네온 3색 각각의 면적 | 화면 전체의 **5% 이내** (선, 글자, 아이콘 등 가는 요소 기준) |
| 네온 3색 합산 면적 | 화면 전체의 **20%를 넘지 않는다** — 배경·표면·글자색이 화면의 대부분을 차지해야 한다 |
| 포인트 컬러 위치 | 링크, 배지, 버튼, 포커스 링, 글로우 효과, 카드 테두리 강조 |
| 색으로만 구분 금지 | 색상 차이 외에 글자나 모양으로도 구분되게 한다 |
| 색 추가 금지 | 위 8개 외 새로운 색을 임의로 추가하지 않는다. 그라디언트도 이 8색 사이에서만 만든다 |
| 순백·순검 회피 | `#FFFFFF`, `#000000` 대신 표에 지정된 글자색·배경색을 쓴다. 극단적 대비보다 은은한 글로우가 톤에 맞는다 |

> 흔한 실수: 네온이 예뻐서 본문 텍스트에까지 색을 입히는 것.
> 정보를 전달해야 하는 텍스트(본문, 과정 서술, 연락처)는 기본 글자색을 유지한다.
> 네온은 "여기를 보라"는 신호이지, 읽는 흐름 전체를 물들이는 배경이 아니다.

---

## 4. 타이포그래피

### 4.1 폰트

| 역할 | 폰트 | 적용 대상 |
|---|---|---|
| 제목 | 굵은 라운드 산세리프 (예: Space Grotesk, Poppins Bold) | 서비스명, 페이지 제목, 프로젝트 제목 |
| 본문 | 가독성 우선 산세리프 (예: Pretendard, Noto Sans KR) | 그 외 **전부** |

**두 폰트 역할 규칙은 그대로다 — 제목 역할 하나, 본문 역할 하나.**
`Coming Soon` 배지나 스티커형 라벨처럼 짧은 문자열에 한해 제목 폰트를 **비트맵/픽셀 느낌으로 스타일링**
(넓은 자간 + 각진 테두리)해 쓸 수 있지만, 이는 제목 폰트의 스타일 변형이지 별도 폰트가 아니다.
실제 픽셀 폰트 파일을 세 번째로 추가하지는 않는다.

### 4.2 크기

| 용도 | 크기 | 굵기 | 폰트 |
|---|---|---|---|
| 인트로 서비스명 | 40–56px | Bold | 제목 |
| 페이지 제목 | 28px | Bold | 제목 |
| 프로젝트 카드 제목 | 20px | Bold | 제목 |
| 본문 | 16px | Regular | 본문 |
| 설명 · 캡션 | 14px | Regular | 본문 |
| 태그 · 뱃지 | 14px | Medium | 본문 |

**본문 14px 미만 금지.** 14px가 하한선이며, 그보다 작은 글자는 어떤 요소에도 쓰지 않는다.

### 4.3 기타 규칙

| 항목 | 규칙 |
|---|---|
| 줄 간격 | 본문 1.7, 제목 1.3 |
| 굵기 | 제목은 Bold(700) 사용. 본문은 Regular(400)와 Medium(500)만 사용, 본문에 Bold 금지 |
| 정렬 | 왼쪽 정렬 기본. 인트로 서비스명만 가운데 정렬 허용 |
| 영문 대문자 | `ENTER` 버튼, `Coming Soon` 배지, 스티커형 라벨, 섹션 소제목에 허용. 본문 문장에는 쓰지 않는다 |
| 한 줄 길이 | 본문은 한 줄 45–75자 사이 |
| 제목 글로우 | 제목 텍스트에 옅은 텍스트 섀도우(글로우)를 줄 수 있다. 본문 텍스트에는 쓰지 않는다 |

---

## 5. 레이아웃

### 5.1 간격

8px 배수만 사용한다. 어중간한 숫자(13px, 27px)를 쓰지 않는다.

| 단계 | 값 | 용도 |
|---|---|---|
| XS | 8px | 태그 사이, 아이콘과 글자 사이 |
| S | 16px | 카드 내부 요소 간격 |
| M | 24px | 카드 내부 여백 |
| L | 40px | 섹션 사이 |
| XL | 80px | 페이지 상하 여백 |

### 5.2 모서리 & 입체감

| 요소 | 값 |
|---|---|
| 카드 | 12px |
| 버튼 · 입력 요소 | 8px |
| 뱃지 · 태그 | 4px |

버튼은 **입체 베벨** 처리를 한다 — 위·왼쪽은 밝은 하이라이트 1px, 아래·오른쪽은 어두운 그림자 1px.
베벨 두께는 항상 1px로 고정한다. 두꺼운 베벨은 로우파이가 아니라 조잡해 보인다.

### 5.3 배경 장식 (격자 · 스캔라인)

| 요소 | 규칙 |
|---|---|
| 격자 | 배경 전체에 아주 옅게(opacity 5–8%) 깔 수 있다. 사이버 블루 1px 선 |
| 스캔라인 | 수평선 반복 패턴. opacity 4–6%. 텍스트 영역 위에는 얹지 않는다 |
| 별·반짝임 | 작은 사각/십자 스파클을 장식 요소로 소량 배치. 정보 영역(카드 내부 텍스트 블록) 안에는 넣지 않는다 |

격자·스캔라인·스파클은 모두 CSS/SVG로 생성한다. 사진, GIF, 영상 소재는 쓰지 않는다.

### 5.4 그리드

| 환경 | 기준 폭 | 카드 배치 | 좌우 여백 |
|---|---|---|---|
| 데스크톱 | 1200px 이하 | 4열 × 2행 | 40px |
| 태블릿 | 768–1024px | 2열 × 4행 | 32px |
| 모바일 | 767px 이하 | 1열 | 20px |

카드 사이 간격은 24px로 통일한다.

---

## 6. 컴포넌트

### 6.1 인트로 페이지

| 요소 | 규칙 |
|---|---|
| 배치 | 화면 정중앙 세로 정렬 |
| 서비스명 | 제목 폰트 40–56px Bold. 옅은 글로우(라임 또는 사이버 블루 중 하나만) |
| 태그라인 | 본문 16px, 보조 글자색 *(문구 미정)* |
| ENTER 버튼 | 입체 베벨 버튼. 배경은 라임 또는 사이버 블루 중 하나로 채운다. 대문자 |
| 버튼 크기 | 최소 높이 48px, 좌우 여백 32px |
| 배경 | 배경색 + 5.3의 격자/스캔라인만. 사진·영상 사용하지 않는다 |
| 배경 움직임 | 격자·스캔라인에 아주 느린 흐름을 줄 수 있다(선택). 필수는 아니다 |

### 6.2 프로젝트 카드

**구성 순서 (위에서 아래로)**

1. `Coming Soon` 배지 — 우상단 고정, 스티커형
2. 프로젝트 제목 — 제목 폰트 20px Bold
3. 분야 태그 — 포인트 컬러 14px
4. 한 줄 설명 — 본문 14px, 보조 글자색

**스타일**

| 항목 | 규칙 |
|---|---|
| 배경 | 표면색 `#171A45` |
| 테두리 | 1px `#34386E` |
| 내부 여백 | 24px |
| 비율 | 8개 모두 동일한 크기와 비율. 예외 없음 |
| 최소 높이 | 180px |
| 카드별 장식 | 회전 각도, 스파클 위치 등 개별 카드마다 다르게 꾸미지 않는다. 8개는 항상 같은 규칙으로 그려진다 |

**`Coming Soon` 배지 (스티커형)**

- 배경 라임, 글자는 배경색(`#0B0E2E`)으로 반전 — 실제 스티커처럼 또렷하게
- 1px 진한 테두리 + 아주 옅은 글로우
- 8개 카드 모두 동일한 위치·크기·각도(회전 없음)
- "준비 중"임을 명확히 표기. 애매한 표현을 쓰지 않는다

**호버**

- 위로 2px 이동 + 테두리가 사이버 블루로 바뀌며 옅은 글로우 발생
- 터치 기기에는 호버가 없으므로, 배지·태그만으로도 상태가 읽혀야 한다

### 6.3 상세 모달

| 항목 | 규칙 |
|---|---|
| 배경 오버레이 | 배경색(`#0B0E2E`)의 70% 불투명도 |
| 모달 배경 | 표면색 `#171A45` |
| 테두리 | 1px `#34386E` + 바깥쪽 아주 옅은 사이버 블루 글로우 |
| 폭 | 최대 720px, 화면의 90% 이내 |
| 내부 여백 | 40px (모바일 24px) |
| 닫기 버튼 | 우상단. 최소 44×44px 클릭 영역 |
| 최대 높이 | 화면의 85%. 넘으면 모달 내부에서만 스크롤 |

준비 중 상태의 모달은 빈 항목을 나열하지 않는다. 안내 문구와 예정 내용만 표시한다.

### 6.4 About 페이지

| 항목 | 규칙 |
|---|---|
| 본문 폭 | 최대 680px. 한 줄이 길어지지 않게 제한 |
| 제목 | 제목 폰트 28px Bold |
| 본문 | 본문 16px, 줄 간격 1.7 |
| 연락처 | 하단에 텍스트 링크로 배치. 버튼으로 만들지 않는다 |
| 링크 | 라임 글자 + 밑줄. About 전체에서 링크색은 라임 하나로 통일한다 |

About은 판단 근거가 되는 페이지이므로, 카드·모달보다 장식(글로우·격자·스파클)을 더 절제한다.

---

## 7. 인터랙션

Y2K 톤에서는 인트로 v1.0보다 움직임을 더 허용하지만, 두 종류를 구분해서 다룬다.

- **상태 전환** — 호버, 클릭, 열기/닫기처럼 사용자 동작에 반응하는 움직임
- **앰비언트 루프** — 글로우 명멸, 마퀴(흐르는 텍스트), 마그네틱 커서 반응처럼 계속 반복되는 움직임

### 7.1 상태 전환 규칙

| 항목 | 값 |
|---|---|
| 지속 시간 | 0.2–0.3초. **0.3초 초과 금지** |
| 가속 곡선 | ease-out |
| 동시 실행 | 한 화면에서 동시에 움직이는 요소는 최소화 |

| 상황 | 동작 |
|---|---|
| 인트로 → 리스트 | 부드러운 페이드 |
| 리스트 진입 | 카드 8개가 **0.05초 간격으로 순차 등장** |
| 카드 마우스 오버 | 위로 2px 이동 + 테두리 네온으로 전환 |
| 카드 클릭 | 모달 페이드인 + 아래에서 8px 올라옴 |
| 모달 닫기 | 페이드아웃 |
| 링크 마우스 오버 | 밑줄 진해짐 |

### 7.2 앰비언트 루프 규칙

| 요소 | 규칙 |
|---|---|
| 글로우 명멸 | 2–4초 주기의 아주 완만한 밝기 변화만 허용. **초당 3회를 넘는 깜빡임은 금지** (광과민 반응 위험) |
| 마퀴(흐르는 텍스트) | 장식용 텍스트에만 사용. 프로젝트 정보·본문 등 판단에 필요한 텍스트에는 쓰지 않는다. 한 바퀴 20초 이상으로 느리게 |
| 마그네틱 커서 반응 | 이동 폭은 최대 8px. 버튼·글자의 실제 클릭 판정 위치는 옮기지 않는다 |
| 마우스 없는 환경 | 터치 기기·키보드 탐색에서는 마그네틱을 적용하지 않는다 |

> 앰비언트 루프는 화면을 "켜져 있는 것"처럼 보이게 하는 장치다.
> 정보를 가리거나 읽기를 방해하면 목적을 잃는다. 헷갈리면 끈다.

### 7.3 접근성

| 항목 | 규칙 |
|---|---|
| 애니메이션 끄기 | 기기 설정에서 동작 줄이기를 켠 사용자에게는 상태 전환과 앰비언트 루프 **모두** 제거한다 |
| 마우스 오버 대체 | 터치 기기에는 오버 효과가 없으므로, 오버로만 정보를 전달하지 않는다 |
| 포커스 표시 | 키보드 이동 시 사이버 블루 외곽선이 명확히 보여야 한다 |
| 클릭 영역 | 모든 클릭 요소 최소 44×44px |
| 명멸 제한 | 어떤 요소도 초당 3회를 넘는 빈도로 깜빡이지 않는다 |

---

## 8. 금지사항

### 8.1 절대 금지

| # | 금지 항목 | 이유 |
|---|---|---|
| 1 | **본문 14px 미만** | 가독성 하한선 |
| 2 | **과장된 자기소개** | 아래 8.3 참조 |
| 3 | 세 번째 폰트(별도 파일) 추가 | 두 폰트 역할 규칙이 무너진다. 픽셀 느낌은 제목 폰트의 스타일 변형으로만 허용 |
| 4 | 팔레트 8색 외 색 추가 | 통일감이 깨진다 |
| 5 | 순백 `#FFFFFF` / 순검 `#000000` 사용 | 지정된 배경·글자색 대신 극단적 대비를 쓰면 톤이 깨진다 |
| 6 | **초당 3회를 넘는 깜빡임/명멸** | 광과민성 반응 위험. 글로우는 완만하게만 |
| 7 | 실제 아티스트·브랜드의 로고, 사진, 그래픽 사용 | 저작권·초상권 문제. 시대 양식만 참고하고 소재는 직접 만든다 |
| 8 | 카드 8개의 크기·모양·회전·장식을 다르게 하기 | 정돈된 인상이 무너진다 |
| 9 | 네온 컬러 3색 중 하나라도 화면의 5% 초과, 합산 20% 초과 | 강조 기능을 잃고 화면이 산만해진다 |
| 10 | 상태 전환 애니메이션 0.3초 초과 | 느리게 느껴진다. (앰비언트 루프는 7.2 별도 규칙 적용) |
| 11 | 본문 텍스트에 Bold(700) 사용 | 제목과 구분이 안 되고 화면이 무거워진다 |
| 12 | 이모지 사용 | 리크루터 대상에 맞지 않는다. 스파클 장식은 이모지가 아니라 직접 그린 SVG로 대체한다 |
| 13 | 인트로 배경에 사진·영상 사용 | 로딩이 느려져 이탈로 이어진다. 격자·스캔라인은 CSS/SVG로만 |

### 8.2 조건부 허용

금지 목록에는 넣지 않되, 아래 조건을 지켜서 쓴다.

| 항목 | 조건 |
|---|---|
| **네온 글로우(box-shadow)** | 버튼·배지·카드 테두리·제목 등 강조 요소에만. 흐림 반경을 넓게, 불투명도는 낮게. 본문 텍스트에는 쓰지 않는다 |
| **베벨(입체 버튼)** | 두께 1px 고정. 두꺼운 베벨은 로우파이가 아니라 조잡해 보인다 |
| **격자·스캔라인 배경** | opacity 4–8% 이내. 텍스트 위에 겹치지 않게 |
| **스티커형 라벨 회전** | 배지·라벨 전체에 한 가지 각도만 통일해서 쓴다(또는 회전 없음). 카드마다 다르게 하지 않는다 |

### 8.3 글쓰기 톤 — 과장 금지

디자인이 화려해진 것과 별개로, 문구는 여전히 담백해야 한다. About과 프로젝트 설명에 아래를 적용한다.

| 금지 | 대신 |
|---|---|
| "열정적인", "성실한", "끊임없이 성장하는" | 구체적 사실 |
| "혁신적인", "완벽한" 같은 과장 형용사 | 실제로 한 일 |
| 근거 없는 자기 평가 | 배우고 있는 것을 그대로 서술 |
| 느낌표 남발 | 담백한 마침표 |

> 예시
> — 나쁨: "끊임없이 도전하며 성장하는 열정적인 개발자입니다!"
> — 좋음: "웹 프론트엔드를 공부하고 있습니다. 요즘은 화면 상태 관리를 어떻게 나눌지에 관심이 있습니다."

화면은 화려해져도 되지만, 글까지 화려해지면 리크루터에게 가장 빨리 걸러지는 신호가 된다.
비주얼로 시선을 끌었다면, 문장으로는 신뢰를 준다.

---

## 9. 점검 체크리스트

화면을 만든 뒤 아래를 확인한다.

- [ ] 순백이나 순검이 쓰인 곳이 없는가
- [ ] 폰트 역할이 제목/본문 두 종류를 넘지 않는가 (픽셀 스타일은 제목 폰트의 변형인가)
- [ ] 네온 3색이 각각 5%, 합산 20%를 넘지 않는가
- [ ] 14px보다 작은 글자가 없는가
- [ ] 카드 8개의 크기·모양·회전·장식이 완전히 동일한가
- [ ] 실제 유니코드 이모지가 없는가 (스파클은 SVG인가)
- [ ] 실제 브랜드·아티스트의 로고·사진·그래픽을 쓰지 않았는가
- [ ] 간격이 모두 8의 배수인가
- [ ] 상태 전환 애니메이션이 0.3초를 넘지 않는가
- [ ] 글로우·명멸이 초당 3회를 넘지 않는가
- [ ] 마퀴가 정보 텍스트가 아니라 장식 텍스트에만 쓰였는가
- [ ] 모든 클릭 요소가 44px 이상인가
- [ ] 동작 줄이기 설정에서 상태 전환·앰비언트 루프가 모두 꺼지는가
- [ ] 키보드만으로 이동할 때 포커스 위치가 사이버 블루로 보이는가
- [ ] 모바일에서 좌우로 밀리는 요소가 없는가
- [ ] 글에 과장된 표현이 없는가

---

## 10. 확정 필요 항목

| 항목 | 상태 |
|---|---|
| 인트로 태그라인 문구 | 미정 |
| 제목 폰트 최종 선정 | 미정 (라운드 산세리프 예시안만 있음) |
| `Coming Soon` 배지 문구 표기 | 미정 |
| 분야 태그 목록 | 미정 (프로젝트 확정 후) |
| 파비콘 · 로고 형태 | 미정 |
| 격자 · 스캔라인 적용 범위 | 미정 (인트로만 vs 전 페이지) |
| 마퀴(흐르는 텍스트) 사용 여부·위치 | 미정 — 쓴다면 장식 문구만 |
| 네온 3색의 최종 톤 | 제안값(라임/핑크/사이버 블루) — 실제 화면에서 대비 확인 필요 |
